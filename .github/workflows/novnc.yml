name: noVNC

on:
  workflow_dispatch:

jobs:
  novnc:
    runs-on: windows-latest

    steps:
      - name: Test Runner
        shell: powershell
        run: |
          Write-Host "================================"
          Write-Host "noVNC workflow started"
          Write-Host "================================"
